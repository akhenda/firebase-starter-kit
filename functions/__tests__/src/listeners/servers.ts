/* eslint-disable jest/no-export */
export default () => {
  describe('Express Servers (HTTP)', () => {
    describe('Questions Server', () => {
      it.todo('GET / ==> returns "Hello, World!" message');
      it.todo('POST /science ==> returns "Hello, Science!" message');
    });
  });
};
